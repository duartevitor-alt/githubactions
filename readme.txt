# this will be the annotations of github action courses:

-- theory (key elements):

	-- workflows
		first thing that you build when starting a project. Can be attatch to the repo.
		Can include one or more jobs
		
	-- jobs
		Can have one or more steps
		Define a runner

	-- steps
		Are really what will be executed (the action itself)
		Execute a shell script or an Action
